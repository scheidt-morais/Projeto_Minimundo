# 📚 Sistema de Gestão da Biblioteca Comunitária "Biblioteca Esperança"

Repositório contendo os scripts SQL do projeto da disciplina de Modelagem e Banco de Dados.

## 🚀 Conteúdo

### 1. Scripts SQL (DML + DDL)
- `create_tables.sql` — criação das tabelas
- `insert_data.sql` — inserção de dados
- `selects.sql` — consultas SQL (JOIN, WHERE, LIMIT, ORDER BY)
- `updates.sql` — operações de atualização
- `deletes.sql` — operações de remoção

### 2. Diagrama Lógico
Arquivo `DER_logico_revisado.png` contendo o modelo lógico revisado (PK e FK).

### 3. Tecnologias Utilizadas
- MySQL Workbench / MariaDB  
ou  
- PostgreSQL + PGAdmin

### 4. Execução
1. Crie o banco de dados com `create_tables.sql`
2. Execute `insert_data.sql`
3. Teste consultas com `selects.sql`
4. Utilize `updates.sql` e `deletes.sql` conforme necessário

### 5. Autor
Trabalho acadêmico para a disciplina de Banco de Dados.
